import type { ExpectedMachine } from "../types.js";

export const expected: ExpectedMachine = {
  component: "MuiNotificationSettings",
  defaultProps: { plan: "free" },
  states: [
    { id: "free_off", description: "plan='free', enabled=false, digest=false; digest checkbox rendered but disabled" },
    { id: "free_on", description: "plan='free', enabled=true, digest=false" },
    { id: "free_on_digest", description: "plan='free', enabled=true, digest=true" },
    { id: "pro_off", description: "plan='pro', all three off" },
    { id: "pro_off_sms", description: "plan='pro', smsAlerts=true while notifications are off — SMS is not gated on `enabled`" },
    { id: "pro_on", description: "plan='pro', enabled=true, digest=false, sms=false" },
    { id: "pro_on_sms", description: "plan='pro', enabled=true, sms=true" },
    { id: "pro_on_digest", description: "plan='pro', enabled=true, digest=true" },
    { id: "pro_on_digest_sms", description: "plan='pro', all three on" },
  ],
  transitions: [
    { from: "free_off", action: "toggle Enable notifications", to: "free_on" },
    { from: "free_on", action: "toggle Enable notifications", to: "free_off" },
    { from: "free_on", action: "toggle Weekly digest", to: "free_on_digest" },
    { from: "free_on_digest", action: "toggle Weekly digest", to: "free_on" },
    { from: "free_on_digest", action: "toggle Enable notifications", to: "free_off", note: "turning notifications off also clears digest" },
    { from: "free_on_digest", action: "click Reset", to: "free_off" },
    { from: "free_on", action: "click Reset", to: "free_off" },
    { from: "pro_off", action: "toggle SMS alerts", to: "pro_off_sms" },
    { from: "pro_off", action: "toggle Enable notifications", to: "pro_on" },
    { from: "pro_off_sms", action: "toggle Enable notifications", to: "pro_on_sms" },
    { from: "pro_off_sms", action: "toggle SMS alerts", to: "pro_off" },
    { from: "pro_on", action: "toggle Weekly digest", to: "pro_on_digest" },
    { from: "pro_on", action: "toggle SMS alerts", to: "pro_on_sms" },
    { from: "pro_on_sms", action: "toggle Weekly digest", to: "pro_on_digest_sms" },
    { from: "pro_on_sms", action: "toggle SMS alerts", to: "pro_on" },
    { from: "pro_on_sms", action: "toggle Enable notifications", to: "pro_off_sms" },
    { from: "pro_on_digest", action: "toggle SMS alerts", to: "pro_on_digest_sms" },
    { from: "pro_on_digest", action: "toggle Weekly digest", to: "pro_on" },
    { from: "pro_on_digest", action: "toggle Enable notifications", to: "pro_off" },
    { from: "pro_on_digest_sms", action: "toggle Weekly digest", to: "pro_on_sms" },
    { from: "pro_on_digest_sms", action: "toggle SMS alerts", to: "pro_on_digest" },
    { from: "pro_on_digest_sms", action: "toggle Enable notifications", to: "pro_off_sms" },
    { from: "pro_on_digest_sms", action: "click Reset", to: "pro_off" },
  ],
  notes: [
    "Material UI's Switch and Checkbox both render a real input[type=checkbox] inside their label, so discoverActions finds them with no portal handling and computes their accessible name from the FormControlLabel text. Switch carries role='switch', so its action id is toggle:switch:\"Enable notifications\" while the checkboxes are toggle:checkbox:...; that is the accessible role, not a special case for MUI.",
    "Under plan='free' the SMS checkbox is not rendered at all, so smsAlerts is a hook that exists in the fiber and can never change — the free_* subgraph is 3 states, and the pro_* states carry generated-props provenance in the multi-assignment report. Same shape as the PropGated benchmark, through a component library.",
    "The digest checkbox is disabled while notifications are off, so at free_off/pro_off its action lands in the engine's `unavailable` list with reason 'disabled' rather than in the graph. Reset from free_off/pro_off is a self-loop.",
    "This benchmark is why src/abstraction/domFingerprint.ts reads `checked`/`value` from the live DOM property rather than the attribute. React sets the property on a controlled input and leaves the attribute at its initial value, and MUI's ticked/unticked difference is otherwise only a class name and an SVG path — so before that fix the fingerprint never changed when a checkbox was ticked, the DOM-correlation pruner deleted digest and smsAlerts as invisible state, and the pro graph collapsed from 6 states to 2. No component in the original corpus used a controlled checkbox, which is why nothing caught it earlier.",
  ],
};
